/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const run = require('./run')
const cases = require('./cases')

let lib = run.bind(this)

Object.assign(lib, run, {
  run,
  cases
})

module.exports = lib
